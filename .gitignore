# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
build/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
lib/
lib64/
parts/
sdist/
var/
wheels/
pip-wheel-metadata/
share/python-wheels/
*.egg-info/
.installed.cfg
*.egg
MANIFEST

# Virtual Environment
.venv/
env/
ENV/
env.bak/
venv.bak/

# Environment variables
.env
.env.local

# Data bestanden (worden tijdens uitvoering aangemaakt)
data/seasonal_baseline.json
data/state.json
data/forecasts_log.jsonl
data/buoy_spectra_history.jsonl
data/bias_log.jsonl
data/*.json
data/*.jsonl

# Forecaster-referentie-archief leeft in een aparte private repo sinds
# public-rollout (auteursrechtelijk materiaal, mag niet publiek).
# Mocht de map nog lokaal bestaan, gewoon negeren:
data/tobias_archive/

# Seasonal baseline MOET in git (rebuild-baseline.yml committeert 'm).
# Zonder dit kan calculate_rarity_percentile geen alerts firen in CI.
!data/seasonal_baseline.json

# SMS-archief: elke verstuurde SMS (digest + alert) wordt per maand
# gepersisteerd in git voor latere model-training (Sprint 4). Public
# want het is onze eigen output, geen Tobias-IP. Auto-commit vanuit
# check.yml na succesvolle send.
!data/sms_archive/
!data/sms_archive/*.jsonl

# IDE
.vscode/
.idea/
*.swp
*.swo
*~

# Testing
.pytest_cache/
.coverage
htmlcov/
*.cover

# Logs
*.log

# OS
.DS_Store
Thumbs.db

# GitHub Actions runner files
.github/workflows/*.bak